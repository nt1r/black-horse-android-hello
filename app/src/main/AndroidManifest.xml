<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.black_horse_onboarding">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BlackHorseOnboarding">
        <activity android:name=".ArchActivity" />
        <activity android:name=".NetworkActivity" />
        <activity android:name=".ViewActivity" />
        <activity android:name=".SubmitActivity" />
        <activity android:name=".SPActivity" />
        <activity
            android:name=".DialogActivity"
            android:theme="@style/dialog_style" />
        <activity android:name=".CounterActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".RecyclerViewHomeActivity" />
        <activity android:name=".LanguageActivity" />
        <activity android:name=".ContactsDetailActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".ContactsHomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ConstraintActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>